apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wp-pv-claim
  labels:
    app: wordpress
  namespace: lc2a_website
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
